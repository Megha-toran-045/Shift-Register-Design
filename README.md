# Shift Register Design using Verilog

This project implements a 4-bit shift register with operations like shift left, shift right, hold, and parallel load. The design is written in Verilog and can be simulated using ModelSim.
